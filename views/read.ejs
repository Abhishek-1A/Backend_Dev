<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
      <a class="text-blue-500" href="/">Back To Home</a>
      <h1 class="text-3xl tracking-tighter mb-3">User</h1>
      <div class="user w-72 p-4 bg-blue-800 rounded-lg">
        <% if (users.length > 0) { %>
          <div class="w-full h-72 rounded-lg bg-blue-700">
            <img class="w-full h-full object-cover object-center" src="<%= users[0].image %>" alt="User Image">
          </div>
          <h3 class="text-xl mt-3 tracking-tighter"><%= users[0].name %></h3>
          <h5 class="text-zinc-500"><%= users[0].email %></h5>
          <div class="flex gap-3 mt-10">
            <a class="text-zinc-200" href="">Edit User</a>
            <a class="text-red-500" href="">Delete User</a>
          </div>
        <% } else { %>
          <p>No user found.</p>
        <% } %>
      </div>
    </div>
  </body>
</html>
